name: CI workflow

on:
  pull_request:
    branches:
      - main
      - dev
  push:
    branches:
      - dev

env:
  NODE_ENV: 22.0.0

jobs:
  unit-test:
    runs-on: ubuntu-latest
    name: Run Unit Test
    steps:
      - id: foo
        uses: dangphu2412/ci-cd-training/.github/quality@v4`
