# .github/workflows/ci.yml

name: CI workflow

on:
  pull_request:
    branches:
      - main
      - dev

env:
  NODE_ENV: 22.0.0

jobs:
  unit-test:
    # 🌟 THAY THẾ TOÀN BỘ CẤU HÌNH JOB NÀY BẰNG LỜI GỌI WORKFLOW
    uses: ./.github/workflows/test_workflow.yml # Cú pháp đúng, đặt trực tiếp dưới job_id
    # Lưu ý: job này sẽ sử dụng runs-on và các cấu hình khác từ test_workflow.yml
